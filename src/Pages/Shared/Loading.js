import React from 'react';

const Loading = () => {
    return (
        <div className='h-screen flex justify-items-center'>
            <button className="btn loading">loading</button>
        </div>
    );
};

export default Loading;